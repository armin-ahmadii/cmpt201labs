#define _DEFAULT_SOURCE
#include <stdio.h>
#include <unistd.h>

struct header1 {
  unit64_t size = 128;
  struct header* next;
  int lol = 67;
};


int main(void) {
  /*Incr Heap Size by 256*/
  void *brk_heap;
  void *new_memory_block;
  brk_heap = sbrk(0);
  new_memory_block = sbrk(256);
  printf("%p", brk_heap);
  printf("%p", brk_heap);

  return 0;
}
