#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>

int main() {
  char *buffer;
  size_t buffsize = 32;
  size_t chars;

  buffer = (char*)malloc(buffsize*sizeof(char));
  printf("Please enter some text: ");
  words = getline();
}
